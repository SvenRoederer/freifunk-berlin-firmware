From: Sven Roederer <freifunk@it-solutions.geroedel.de>
Date: Wed, 8 Apr 2020 00:38:18 +0200
Subject: freifunk-common: drop reloading of dnsmasq by crontab

this will be converted to "olsrd sighup" callback

diff --git a/contrib/package/freifunk-common/files-common/etc/init.d/freifunk b/contrib/package/freifunk-common/files-common/etc/init.d/freifunk
index 8ff3138b4cb5f38ee7befeecdb6af36653d21940..f401075f418cb5ee268f7666e9723a6806c70e38 100755
--- a/contrib/package/freifunk-common/files-common/etc/init.d/freifunk
+++ b/contrib/package/freifunk-common/files-common/etc/init.d/freifunk
@@ -5,17 +5,9 @@
 START=99
 
 boot() {
-	test -f /etc/crontabs/root || touch /etc/crontabs/root
-
-	grep -q 'killall -HUP dnsmasq' /etc/crontabs/root || {
-		echo "*/5 * * * *	killall -HUP dnsmasq" >> /etc/crontabs/root
-	}
-
 	[ -d /etc/rc.local.d ] && {
 		for file in /etc/rc.local.d/*; do
 			test -f "$file" && . "$file"
 		done
 	}
-
-	/etc/init.d/cron restart &
 }
